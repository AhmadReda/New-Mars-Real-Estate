<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="Theme.Dark" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="background_color">@color/dark_background</item>
        <item name="background_color_active">@color/dark_background_active</item>
        <item name="text_color">@color/dark_text_color</item>
        <item name="stroke_color">@color/dark_stroke_color</item>
    </style>

</resources>